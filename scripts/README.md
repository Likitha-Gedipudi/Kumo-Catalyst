# Scripts

This folder is reserved for optional local utilities. The repo does not ship offline LinkedIn or Google Takeout graph pipelines; the production path is the Python sidecar under `kumo-sidecar/` with the bundled H&M dataset.
